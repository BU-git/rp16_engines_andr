<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/colorMainBackground"
    android:orientation="vertical"
    tools:context="com.bionic.kvt.serviceapp.activities.LMRAActivity">

    <ListView
        android:id="@+id/lmra_list"
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        tools:listitem="@android:layout/activity_list_item"
        android:dividerHeight="@dimen/activity_vertical_margin"
        android:divider="@color/colorMainBackground"
        android:descendantFocusability="blocksDescendants">
    </ListView>

    <ImageButton
            android:layout_width="90dp"
        android:layout_height="90dp"
        android:id="@+id/button_lmra_new"
        android:background="@color/colorLoginGroup"
        android:src="@drawable/lmra_action_add"/>

</LinearLayout>
